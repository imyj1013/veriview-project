Flask==2.3.2
requests==2.32.2
numpy==1.26.4
opencv-python==4.10.0.84
pandas==1.5.3
pyjwt==2.6.0

# 음성 처리 및 AI 모델에 필요한 패키지들
openai-whisper==20240930
librosa==0.10.2.post1
soundfile==0.12.1
sounddevice==0.5.0
scipy==1.14.1

# PyTorch 및 오디오 처리
torch>=2.4.1
torchaudio>=2.4.1

# TTS (텍스트-투-스피치) 관련 패키지
# 공식 Coqui TTS 패키지 사용
TTS>=0.22.0
# 대체적으로 최신 버전을 위해 git 버전도 지원:
# git+https://github.com/coqui-ai/TTS.git

# 기타 유틸리티
flask-cors==4.0.0
unidecode==1.3.8

# TTS 지원을 위한 추가 패키지들
pydub>=0.25.1
numba>=0.56.0
psutil>=5.9.0

# 설치 주의사항:
# 1. 설치 전에 pip 업그레이드: pip install --upgrade pip
# 2. 일부 환경에서는 Visual Studio Build Tools가 필요할 수 있음
# 3. CUDA 지원을 원하면 PyTorch CUDA 버전 설치 규요
